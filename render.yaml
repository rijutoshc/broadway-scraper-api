services:
  - type: web
    name: broadway-api
    env: python
    plan: free
    buildCommand: |
      ./render-build.sh
      pip install -r requirements.txt
    startCommand: gunicorn main:app

	  
